export function signUpAction(email: string, password: string, confirmHash: string) {   
    console.log("Registering user:", email, password, confirmHash);
    // Implement registration logic here, e.g., API call to register the user
}