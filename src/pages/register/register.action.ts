export function registerAction(email: string, password: string, confirmPassword: string) {   
    console.log("Registering user:", email, password, confirmPassword);
    // Implement registration logic here, e.g., API call to register the user
}